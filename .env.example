# Root env consumed by docker-compose. Copy to .env and fill in.

# Scraper LLM key — set ONE. Anthropic (claude-haiku-4-5) takes precedence.
ANTHROPIC_API_KEY=
GEMINI_API_KEY=

# Required: shared secret to trigger POST /api/v1/scrape.
SCRAPE_API_KEY=change-me
